/**
 * Application Constants
 */

// Application Version (semver)
export const APP_VERSION = "0.1.0";

// Data Export Format Version (semver)
// Increment this when the structure of ExportData changes
export const DATA_EXPORT_VERSION = "1.0.0";

// Minimum Data Version supported by this version of the app
// Older backups with versions lower than this will be rejected
export const MIN_SUPPORTED_DATA_VERSION = "1.0.0";
